# AttendanceAnalysis

This Python project analyzes student attendance.

## Dataset
Columns: Name, Attendance (%)

## Features
- Finds students with low attendance (<75%)
- Displays a pie chart of attendance distribution

## How to Run
1. Install Python dependencies: `pandas` and `matplotlib`
2. Run the script: `python attendance_analysis.py`
